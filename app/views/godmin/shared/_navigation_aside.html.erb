<% if authentication_enabled? &&
      admin_user.singleton_class.include?(Godmin::Authentication::User) &&
      admin_user_signed_in? %>
    	<%= link_to('<md-button hide-xs hide-sm>Logga ut</md-button>'.html_safe, session_path, :method => :get) %>
<% end %>

